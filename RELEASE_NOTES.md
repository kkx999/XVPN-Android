# XVPN Android 1.1.0

第六个正式版候选源码，基于 RC5 真机反馈修复测速一致性、自定义联网目标、流量统计与底部面板，并新增出口 IP 展示。

## 网络与稳定性

- 智能分流按私网 / 本地、中国域名、中国 IP、最终代理的顺序匹配；全局模式仅保留局域网直连。
- 国内域名与节点域名使用直连 `223.5.5.5` UDP，其他域名使用经代理隧道的 `8.8.8.8 TCP DNS`；DNS 仅返回 IPv4，TUN MTU 固定为 1400。
- 拒绝网页 UDP/443，使 UDP 不可用节点上的 QUIC 请求立即回落 TCP/TLS；不会拦截 Core 自身的 Hysteria2 / TUIC 出口。
- 首次连接在发布“已连接”前请求保存的 HTTP/HTTPS 联网检测网站；热切换保留失败重试与自动回滚，避免假连接。
- 首次健康检查只在失败后补一次短重试，吸收 Android TUN 与 DNS 刚启动时的瞬时未就绪，正常成功路径不增加等待。
- Wi-Fi / 蜂窝网络变化时同时更新 sing-box 默认接口与 Android VPN underlying network，避免隧道残留在已经断开的物理网络上。
- 支持 VLESS、Trojan、VMess、Shadowsocks、Hysteria2、TUIC、AnyTLS 分享配置及常见 TLS / REALITY / transport 参数。
- 首次连接、模式切换、手动换节点和连接诊断统一使用用户保存的联网检测网站；旧 gstatic 内置值迁移到 Apple `success.html`。

## 交互与系统集成

- 已连接时可直接切换智能 / 全局、打开节点列表、手动切换或自动优选。
- 节点卡片、详情与自动优选统一使用不包含 DNS 时间的物理入口 TCP 握手延迟；连接态不再通过当前隧道回退，因此不会产生几毫秒假结果。
- 已连接首页单点测速与节点抽屉使用同一物理网络 Socket 工厂，避免当前节点再次误报“不可达”；最终只切换一次最快节点，并使用保存的网站完成真实联网验证。
- 默认联网检测地址为 Apple `success.html`；地址保存后立即用于全部隧道健康流程，HTTP 4xx/5xx 不再被误计为成功。
- 首页新增出口 IP 冰晶卡片，显示公网 IP、运营商、机房 / 家宽与地区匹配的保守智能识别，并可点击复测。
- 恢复 admin 流量上报，同时增加本机日 / 月 / 累计流量账本作为 Panel 统计缺失时的显示回退。
- 底部节点面板与 Android 导航手势区使用同色背景及主题化图标，降低小白条的视觉割裂。
- 连接球在已连接状态使用独立薄荷绿冰晶边缘；底部卡片、测速反馈、主题切换与深色模式统一短动效。
- 右上角刷新按钮改为主题自适应圆形冰晶刷新控件，浅色与深色分别控制底色、细边和动态高光。
- 窄屏与系统显示放大时自动收紧连接球、卡片、间距和字体比例，大屏手机保持原有视觉尺寸。
- 前台通知显示节点、模式、连接时长、实时速率、健康状态与安全断开操作。
- 默认 Panel 使用 `https://xvpn.666101.xyz`；RC2 从此地址建立托管基线，后续只自动迁移仍使用内置地址的用户，不改写自定义地址。
- Panel 或网关返回 HTML 5xx 页面时，客户端只显示明确的中文错误，不再把服务器 HTML 原文渲染到登录页。
- Panel 更新接口故障时回退 GitHub Latest Release；服务端明确暂停更新时不绕过策略。

## 发布要求

GitHub Actions 会执行 14 份协议/分流配置生成测试、lint、签名构建、zipalign、APK v2 签名、包名/版本/ABI、单 DEX、规则资产和固定证书检查。创建 `v1.0.0` 正式 Release 前，仍须按 `QA_CHECKLIST.md` 在实际节点与 Android 真机上完成协议握手、分流、DNS、通知和长时间运行验收。
